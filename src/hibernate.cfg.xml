<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings (request)-->
        <property name="connection.driver_class">org.sqlite.JDBC</property>
        <property name="connection.url">jdbc:sqlite:test2.db</property>
        <property name="connection.username">root</property>
        <property name="connection.password">root</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">2</property>

        <!-- SQL dialect (request)-->
        <property name="dialect">com.sio.hibernate.dialect.SQLiteDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

      <!--
		 
		 <mapping class="siox.entity.Viewer"/>
		 <mapping class="siox.entity.Cart"/>
		 <mapping class="siox.entity.Connector"/>
		 <mapping class="siox.entity.Db"/>
		 <mapping class="siox.entity.Elemental"/>
		 <mapping class="siox.entity.Log"/>
		 <mapping class="siox.entity.Tag"/>
		 <mapping class="siox.entity.Template"/>
		 <mapping class="siox.entity.User"/>
		 <mapping class="siox.entity.Bind"/>
		 
		-->
		
		<mapping class="com.sio.hibernate.entity.Tag"/>
    </session-factory>

</hibernate-configuration>